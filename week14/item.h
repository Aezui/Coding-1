#pragma once
using namespace std;


/*
homework : 
1. create a function in filegmt.cpp to read a file into an array(done)
a. each new line becomes a new element in the array(done)

2. finish the item class(done)

3. in main, create an array of 5 items and display them with a loop
a. use a itemArray[i].examine() to show all of the details of the item.

4. Upload tge folder of files to github
a. sumbit a link to the assignment page with the folder in github.com.
class item
{
    private:
        location location;          // where the item is stored
    public:
        item();             // constructor
}; */

